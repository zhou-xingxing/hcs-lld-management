# TODO
## 网络平面支持多级树状结构
- [x] 比如一个大的网络平面A(地址段掩码为22位)内可包含细分网络平面B和C(均为24位掩码地址段)
- [x] 最多支持3级嵌套关系


## 支持配置定时备份任务
- [x] 备份的是数据库文件
- [x] 支持配置是否开启备份任务，以及备份周期（每天、每周）
- [x] 允许自定义备份方式（备份到本地文件、备份到标准对象存储(用户需配置AK、SK、bucket)）

## 数据模型
- [x] Region的网络平面里加入vlanid（可选），网关位置（可选），网关IP（可选，公网私网自动推荐和弱校验网关IP）
- [x] 考虑Region内的网络平面增加作用域属性，因为可能存在一种网络平面在一个Region内有不同实例，例如external_om在管理AZ和网络AZ下都存在

## 认证登录及权限管理
- [x] 两种用户：administrator和user
- [x] 两种用户对所有数据（业务、配置数据）都有读权限
- [x] administrator可管理用户及权限分配，可管理全局配置，但不可管理Region内的业务数据
- [x] user拥有对所属Region的业务数据的全部管理权限


## 后续可扩展方向
- 前端如果有逻辑代码，也需要加上测试用例
- **PostgreSQL 迁移**：支持空间索引优化 CIDR 查询
- **审计增强**：变更日志支持 Entity 关联查询、差异对比
- **批量操作**：支持批量编辑/删除 Region 网络平面
- **可视化**：Region 网络平面可视化拓扑图
- **导入模板增强**：自动识别 Excel 列名、支持多 sheet
